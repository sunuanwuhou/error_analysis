# 考试提分系统版本变更记录

## 2026-04-03

### 新增项目规则
- 新增规则：涉及页面交互、布局、入口位置、弹层、覆盖层、跳转关系等前端交互设计时，默认同步给出对应交互图或结构示意图
- 新增规则：项目推进过程中，每形成阶段性结论后，同步更新对应项目文档
- 新增规则：项目优先按“项目层 → 模块层 → 功能层 → 细节层”推进，避免碎片化开发
- 新增规则：前端改造前，必须先确认“当前运行入口 / 本次修改入口 / 页面验收入口”
- 新增规则：若当前在线页面由 `v51_frontend` 驱动，则页面可见类改动默认优先落在 `v51_frontend`

### 确认项目方向
- 确认：系统只是辅助工具，核心目标始终是考试提分
- 确认：当前阶段优先完成并优化系统本身，系统做好后再继续大规模录入笔记
- 确认：围绕项目时，优先给出完整方案、明确规则/边界/验收、按模块推进、每步自测并交付可用结果

### 确认设计基线
- 确认画布 V1 基线：
  - 题目卡片右上角增加“画布”按钮
  - 与“展开”处于同一区域
  - 点击后直接在题目内容区域上覆盖透明画布层
  - 不跳转页面
  - 第一版工具仅保留：撤销 / 清空 / 退出
  - 每道题的画布数据自动保存并自动恢复

### 本次问题结论
- 已确认：当前在线运行入口是 `v51_frontend`
- 已确认：此前 v11 相关补丁主要落在 `frontend/src`
- 已确认：本次问题本质是“改动链路与运行链路不一致”，不是“补丁没写进去”

### 本次修复性规则沉淀
- 后续前端页面改造默认先改当前运行壳，再考虑是否回灌到主源码链路
- 每次补丁说明必须写清：
  - 当前运行入口
  - 本次修改入口
  - 页面验收入口
  - 是否会立即在当前页面生效

### 文档体系调整
- 建立长期维护的 4 份核心文档：
  - 项目总协作规范
  - 已确认设计方案汇总
  - 最新项目状态摘要
  - 版本变更记录

---

## 后续追加模板

## YYYY-MM-DD

### 本次完成
- 

### 本次确认
- 

### 本次变更
- 

### 本次修复
- 

### 下一步
- 


## 2026-04-03（画布终极整合版追加）

### 本次完成
- 基于当前运行入口 `v51_frontend` 交付画布终极整合版
- 一次性落地 Phase 1 / 2 / 3 / 4 效果
- 题面透明覆盖画布、增强工具、导出联动、学习记录联动全部接入

### 本次确认
- 当前页面可见类改动继续以 `v51_frontend` 为准
- 导出链路默认带出 `processCanvas` 原始数据与统计字段

### 本次变更
- 新增 `v51_frontend/assets/process-canvas-ultimate.js`
- 新增 `v51_frontend/assets/process-canvas-ultimate.css`
- `v53-bootstrap.js` 增加终极画布资源加载

### 本次修复
- 不再依赖旧的 iframe 过程图路线作为主入口
- 当前页面直接提供右上角原生画布入口

### 下一步
- 浏览器实机回归与交互微调


### 2026-04-03 画布 V2 体验收敛补丁
- 调整：画布入口从底部操作区回到题目卡片右上角
- 调整：默认只保留一个“画布”入口按钮
- 调整：编辑态主工具只保留 撤销 / 清空 / 退出
- 调整：导出图片移入“更多”二级菜单
- 调整：默认单色画笔，不再显示多颜色按钮
- 调整：保存方式改为自动保存
- 调整：状态文案改为 未标注 / 自动保存中 / 已保存
- 修复：导出图片优先包含题目内容与画布笔迹，失败时回退为仅画布
- 修复：保存后状态联动不稳定的问题
- 优化：自动保存不再强依赖整页 renderAll，降低编辑中断风险
